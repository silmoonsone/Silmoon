@page "/silmoonconfigtest"
@using Silmoon.AspNetCore.Services
@using Silmoon.AspNetCore.Test.Services
<h3>SilmoonConfigTestRp</h3>
@(((SilmoonConfigureServiceImpl)SilmoonConfigureService).MongoDBConnectionString)
@code {
    [Inject]
    ISilmoonConfigureService SilmoonConfigureService { get; set; }
    protected override Task OnInitializedAsync()
    {
        return base.OnInitializedAsync();
    }
}
